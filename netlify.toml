[build]
  # Static site — nothing to compile, publish the repo root as-is.
  publish = "."
  command = ""

# Cache headers also live in _headers; kept here so they survive
# a publish-directory change.
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
